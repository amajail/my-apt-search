name: villa_urquiza
source: argenprop       # MercadoLibre is walled (see source-access-blocker.md); Argenprop is the MVP source
operation: venta
currency: USD          # USD-only; ARS-priced listings are ignored (no conversion)
price_max: 115000      # USD ceiling
rooms: 2               # exactly 2 ambientes
min_area_m2: 40        # covered area (superficie cubierta), matched strictly greater-than
neighborhoods:         # Argenprop barrio slugs (joined with "-o-" in the search URL)
  - "villa-urquiza"    # Villa Urquiza (idbarrio 39)
  - "villa-ortuzar"    # Villa Ortúzar
  - "coghlan"          # Coghlan
